{
  "name": "explore-forge-assets-import",
  "version": "1.1.1",
  "private": true,
  "license": "Apache-2.0",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "changelog": "git cliff",
    "check": "npm run format:check && npm run lint && npm run typecheck && npm run test",
    "clean": "rm -rf ./dist",
    "format": "biome format --write",
    "format:check": "biome format",
    "generate:types": "openapi-typescript docs/jsm-assets-docs/content/cloud/assets/swagger.json -o src/types/assets-api-generated.d.ts",
    "lint": "npm run lint:check",
    "lint:check": "biome lint && forge lint",
    "lint:fix": "biome lint --write",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:watch": "vitest watch",
    "dev:todo": "grep -rn 'TODO' --exclude='package.json' . --color=auto || echo 'No TODOs found!'",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@forge/api": "^8.0",
    "@forge/bridge": "^6.0",
    "@forge/events": "^3.0",
    "@forge/kvs": "^2.0",
    "@forge/react": "^12.0",
    "@forge/resolver": "^2.0",
    "neverthrow": "^8.2",
    "react": "^18.3"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5",
    "@types/node": "^24",
    "@vitest/coverage-v8": "^4.1",
    "archunit": "^2.1",
    "openapi-typescript": "^7.13",
    "typescript": "^5.9",
    "vitest": "^4.1",
    "yaml": "^2.9"
  }
}
